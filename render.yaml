services:
  - type: web
    name: my-flask-app
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: gunicorn app:app --timeout 300
